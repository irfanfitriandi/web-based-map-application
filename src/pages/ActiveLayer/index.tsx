import Layout from "../../components/Layout";
import MapComp from "../../components/Map";

const ActiveLayer = () => {
  return (
    <Layout>
      <MapComp />
    </Layout>
  );
};

export default ActiveLayer;
